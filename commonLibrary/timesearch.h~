#include<iostream>
using namespace std;
class timeresearch(){
	public:
		timeresearch();
		bool update2ndResors_GivenFidelity(double curResors, double curFidR,  double targetFidR);
  		double lastResors;
		double lastFidR;
		double lastDelta;
		const static fidError=0.2;
};
